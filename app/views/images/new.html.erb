<%= simple_form_for @image, defaults: { input_html: { class: 'form-control' } } do |i| %>
  <%= i.input :title %>
  <%= i.input :link %>
  <%= i.input :tag_list %>
  <%= i.button :submit, class: 'btn-primary btn-lg'  %>
<% end %>
